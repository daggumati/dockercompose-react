sudo: required
services:
  - docker
before_install:
  - docker build -t daggumati/dockercompose-react -f Dockerfile.dev .
script:
  - docker run -e CI=true daggumati/dockercompose-react npm run test